[Unit]
Description = FastAPI app
After = network.target

[Service]
User = {{ansible_user}}
WorkingDirectory=/opt/fastapi_app  
ExecStart=/opt/fastapi_app/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8080
Restart = always

[Install]
WantedBy=multi-user.target